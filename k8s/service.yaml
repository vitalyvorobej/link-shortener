
apiVersion: v1
kind: Service
metadata:
  name: link-shortener-service
  namespace: lshort
spec:
  type: NodePort
  selector:
    app: link-shortener
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
      nodePort: 30001